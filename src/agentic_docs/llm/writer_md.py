"""Coordinator to retrieve context and call Markdown writer for impacted symbols."""
def generate_markdown_for_changes(changed_only: bool, dry_run: bool, write: bool):
    print(f"[stub] generate_markdown_for_changes(changed_only={changed_only}, dry_run={dry_run}, write={write})")
