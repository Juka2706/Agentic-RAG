"""Toy module A."""

def add(x: int, y: int) -> int:
    """Return x + y."""
    return x + y

class Greeter:
    """Say hello."""
    def hello(self, name: str) -> str:
        return f"Hello, {name}!"
